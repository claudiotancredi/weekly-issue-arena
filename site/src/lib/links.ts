/**
 * Canonical off-site links.
 *
 * The consent forms are the only way into (and out of) the arena, so
 * their URLs are defined once here and reused by every call-to-action.
 */
export const REPO_URL = "https://github.com/claudiotancredi/weekly-issue-arena";

export const JOIN_ARENA_URL = `${REPO_URL}/issues/new?template=join_the_arena.yml`;

export const LEAVE_ARENA_URL = `${REPO_URL}/issues/new?template=leave_the_arena.yml`;

export const DISCUSSIONS_URL = `${REPO_URL}/discussions`;
